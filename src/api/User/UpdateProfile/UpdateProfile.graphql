type UpdateProfileResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  UpdateProfile(
    firstName: String
    lastName: String
    email: String
    password: String
    age: Int
    profilePhoto: String
  ): UpdateProfileResponse!
}
